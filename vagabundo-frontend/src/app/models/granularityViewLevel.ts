/**
 * Represents at which level of granularity we want to display data. Either for the traveller personally, for the
 * team this traveller belongs to or for the whole organization/company.
 */
export enum GranularityViewLevel {
    Traveller,
    Team,
    Company
}
